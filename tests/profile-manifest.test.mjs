import test from 'node:test';import assert from 'node:assert/strict';import {createProfileManifest,validateProfileManifest} from '../functions/_lib/profile-manifest.js';test('profile manifest is deterministic and validated',async()=>{const a=await createProfileManifest({name:'Synthetic',email:'synthetic@example.test',text:'resume'});const b=await createProfileManifest({name:'Synthetic',email:'synthetic@example.test',text:'resume'});assert.equal(a.revision,b.revision);assert.equal(validateProfileManifest(a),true);});
